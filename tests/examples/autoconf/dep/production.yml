version: '2'

services:
  testapp:
    image: testapp
    build:
      context: ..
      dockerfile: dep/production.Dockerfile
    user: ubuntu

  nginx:
    image: testapp-nginx
    build:
      context: ..
      dockerfile: dep/production.nginx.Dockerfile
